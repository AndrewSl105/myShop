//import { RECEIVE_API_DATA } from './actions';

const initialState = {
  styles: 
    {
      width: "30px",
      height: "40px"
    }
  
}

export default (state = initialState, { type, data }) => {
  switch (type) {
    default:
      return state;
  }
};
